<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Valentine 💘</title>
  <style>
    body {
      background: #ffb6c1;
      font-family: Arial, sans-serif;
      text-align: center;
      padding-top: 70px;
      overflow: hidden;
    }

    h1 { font-size: 42px; }
    #question { font-size: 28px; margin-bottom: 30px; }

    button {
      font-size: 22px;
      padding: 14px 28px;
      border: none;
      border-radius: 14px;
      cursor: pointer;
      position: absolute;
    }

    #yes {
      background: red;
      color: white;
      left: 40%;
      transition: all 0.4s ease;
    }

    #no {
      background: white;
      color: black;
      left: 55%;
    }
  </style>
</head>
<body>

  <h1>Hello sexy 😘</h1>
  <div id="question">Will you be my Valentine? 💕</div>

  <button id="yes" onclick="yesClicked()">Yes 💖</button>
  <button id="no" onmouseover="moveNo()" onclick="growYes()">No 🙄</button>

  <script>
    let scale = 1;

    function moveNo() {
      const noBtn = document.getElementById("no");
      const x = Math.random() * (window.innerWidth - 100);
      const y = Math.random() * (window.innerHeight - 100);
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    }

    function growYes() {
      scale += 0.5;
      const yesBtn = document.getElementById("yes");
      yesBtn.style.transform = `scale(${scale})`;

      if (scale >= 4) {
        yesBtn.style.position = "fixed";
        yesBtn.style.top = "0";
        yesBtn.style.left = "0";
        yesBtn.style.width = "100vw";
        yesBtn.style.height = "100vh";
        yesBtn.style.fontSize = "50px";
        yesBtn.innerText = "YES 💘";
        document.getElementById("no").style.display = "none";
      }
    }

    function yesClicked() {
      document.body.innerHTML = `
        <h1>I know u would say yes 😌💘</h1>
        <img src="https://media.tenor.com/2roX3uxz_68AAAAC/teddy-bear-kiss.gif"
             style="width:300px; margin-top:20px;">
      `;
    }
  </script>

</body>
</html>
